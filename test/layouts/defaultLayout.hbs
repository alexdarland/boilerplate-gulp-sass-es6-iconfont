<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ model.siteName }} | {{ model.title }}</title>
</head>
<body>
{{#each model.content}}
	{{> (lookup . 'view') model = model }}
{{/each}}
</body>
</html>
